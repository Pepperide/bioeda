syntax = "proto3";

option go_package = "github.com/Pepepride/bioda/operators/cmd/slurm-controller;proto";

message JsonRequest {
  string filename = 1;
  bytes json = 2;
}

message SubmitResponse {
  bool ok = 1;
}

service Jobs{
  rpc Submit(JsonRequest) returns (SubmitResponse);
}